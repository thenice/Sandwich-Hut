<div class="pageTitle">
  <h1>MenuItemDefaults</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @menuitemdefaults.each do |menuitemdefault| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => menuitemdefault.object %>">
            <span class="title"><%= menuitemdefault.menu_item_id %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>