<div class="pageTitle">
  <h1>Our Menu</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @categories.each do |category| %>
    
        <li>
          <a href="<%= url_for :controller => :Category, :action => :show, :id => category.object %>">
            <span class="title"><%= category.name %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>