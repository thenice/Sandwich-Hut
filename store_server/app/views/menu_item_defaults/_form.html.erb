<%= form_for(@menu_item_default) do |f| %>
  <% if @menu_item_default.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@menu_item_default.errors.count, "error") %> prohibited this menu_item_default from being saved:</h2>

      <ul>
      <% @menu_item_default.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :menu_item_id %><br />
    <%= f.text_field :menu_item_id %>
  </div>
  <div class="field">
    <%= f.label :ingredients_json %><br />
    <%= f.text_area :ingredients_json %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
